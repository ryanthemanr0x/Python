# Tuples - immuteable = encased in ()
# Difference with list, encased in []

coordinates = (4, 5)

# Try to change tuple = err
#coordinates [1] = 10

print(coordinates[0])
print(coordinates[1])

coordinates1 = [(4, 5), (6, 7), (80, 34)]
print(coordinates1[1])
